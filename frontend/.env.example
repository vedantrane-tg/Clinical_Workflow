# Base URL for the Clinical Referral API.
# In development, Vite proxies /api/* → http://localhost:8000 automatically.
# Override for production or remote backends:
# VITE_API_BASE_URL=https://api.example.com

# Optional label shown in the UI environment badge
# VITE_ENVIRONMENT_LABEL=Production
